export enum direction {
  NORTH,
  EAST,
  SOUTH,
  WEST,
}

export enum motion {
  FORWARD,
  RIGHT,
  BACKWARD,
  LEFT,
}

export enum cameraDirection {
  NORTH,
  EAST,
  SOUTH,
  WEST,
}

export enum motionDirection {
  NORTH,
  EAST,
  SOUTH,
  WEST,
}
